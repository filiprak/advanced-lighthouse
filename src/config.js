module.exports = {
    extends: 'lighthouse:default',
    plugins: [
        './lighthouse-plugin-simulation'
    ],
    settings: {
        // keep default settings or adjust as needed
    },
    
};
